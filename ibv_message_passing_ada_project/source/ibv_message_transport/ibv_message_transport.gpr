project Ibv_Message_Transport is

   for Source_Dirs use (".");
   for Object_Dir use "../../obj/ibv_message_transport";

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-gnato", "-gnatwa", "-gnatQ", "-gnat2012");
   end Compiler;

   package Builder is
      for Default_Switches ("ada") use ("-g");
   end Builder;

   package Ide is
   end Ide;

end Ibv_Message_Transport;
